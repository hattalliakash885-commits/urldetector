# urldetector
A 100% serverless web application that performs heuristic analysis on URLs to detect phishing, malware IPs, and zero-day cyber threats directly in the browser using Cloudflare's DNS-over-HTTPS (DoH) API.
